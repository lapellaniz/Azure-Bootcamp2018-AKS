{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/src/ImageProcessor.Service/ImageProcessor.Service.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}